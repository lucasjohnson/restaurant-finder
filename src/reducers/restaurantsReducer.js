export const initialState = {
  restaurants: [],
  loading: false,
  hasErrors: false
}

export default function restaurantsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
